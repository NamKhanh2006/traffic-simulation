package com.myteam.traffic.rule;

/*
public class SpeedRule implements TrafficRule {
    private Double minSpeed, maxSpeed; // null = không ràng buộc

    public boolean isAllowed(Vehicle v, Action a, RoadContext c) {
        double s = v.getSpeed();
        if (minSpeed != null && s < minSpeed) return false;
        if (maxSpeed != null && s > maxSpeed) return false;
        return true;
    }
}
*/